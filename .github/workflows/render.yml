services:
  - type: web
    name: stream-resolver-api
    env: python
    buildCommand: "pip install -r requirements.txt && playwright install chromium"
    startCommand: "python app.py --serve"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
